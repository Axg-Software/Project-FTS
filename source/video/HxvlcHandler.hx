//For some reason I couldent get it fuckin' working so u guys just have to suffer with a still png for now
package;

/*#if android
import android.widget.Toast;
#end
import flixel.FlxG;
import haxe.Exception;
import hxvlc.flixel.FlxVideo;
import hxvlc.flixel.FlxVideoSprite;
import hxvlc.util.Handle;
import openfl.system.System;
import openfl.utils.Assets;
import openfl.Lib;
#if sys
import sys.io.File;
import sys.FileSystem;
#end

using haxe.io.Path;

class HxvlcHandler extends FlxVideoSprite
{
    public function newVideo() {
        var video:FlxVideoSprite = new FlxVideoSprite(0, 0);
		video.bitmap.onFormatSetup.add(function():Void
		{
			video.setGraphicSize(FlxG.width * 0.7, FlxG.height * 0.7);
			video.updateHitbox();
			video.screenCenter();
		});
    }
}*/